management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
    #git의 자세한 정보까지 표기
    #git:
      #mode: 'full'
  endpoint:
    shutdown:
      enabled: true
    health:
      show-components: always
      #show-details: always
  endpoints:
    web:
      exposure:
        include: "*"

info:
  app:
    name: hello-actuator
    company: yh